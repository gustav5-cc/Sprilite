Simply navigate to the directory these files are in on your command line,
then type "start .\index/html" to open up the converter program. 

Using it is fairly simple, just upload images, preferably sprites and it 
will do the conversions automatically, downloading them if needed.